# 前六项问题修复与复验（2026-09-05）

## 结论

用户批准的前六项已实施并完成自动化与浏览器复验。沿用现有 Cordis 插件架构和同一份 DSH 风格 PageCraft 包，没有创建 AI Agent 专用插件副本。第七项 Skill 来源标签未修改。

| 原问题 | 本次修复 | 验证 |
| --- | --- | --- |
| QA-06：Windows `spawn npm ENOENT` | 独立 argv；Windows npm/npx 解析为 Node CLI；区分未启动、退出失败、超时、取消并保留输出；停止终止命令进程树 | 空格目录中的真实 npm、特殊参数、退出码、超时及取消测试通过；试验仓库 npm 已实际启动 |
| QA-07：API 契约大量假阴性 | TypeScript AST 提取 Axios/Fetch、实例和配置、Router 挂载及常见默认导出；条件分支相关性；循环与动态项明确未解析 | 9 项专项测试；Conduit 17 个可解析调用全部匹配，0 个未匹配调用，1 个动态调用未解析 |
| QA-01：原生目录窗口被浏览器遮挡 | 默认改为网页内目录选择；绝对路径输入、加载/错误状态、请求版本与所属会话校验 | 右侧浏览器选中真实 Conduit 目录并保存，无浏览器外原生窗口 |
| QA-02：草稿跨会话串用 | 按会话及浏览器标签页持久化；请求接受后仅清空提交时版本，保留后续输入 | A/B 草稿切换、整页刷新恢复实测通过；存储失败与过期接受单测通过 |
| QA-03：PageCraft 默认预览自身 | 空历史与空地址；无目标不挂载预览；恢复时抑制宿主自身地址；显式导航仍可用 | 新 session-004 显示空预览，导航/选择工具禁用；选目录后文件树可用；53 项插件测试及原包安装激活测试通过 |
| QA-04：取消状态刷新后丢失 | 原子运行记录、终态不可覆盖、消息关联、部分输出恢复；取消等待落盘再继续 FIFO 后继；服务重启遗留运行标记 interrupted | 队列、生命周期、重启恢复与 pre-agent 取消测试通过；浏览器停止后刷新仍保留部分清单和“操作已取消” |

## 自动化结果

- `npm test`：81 通过，0 失败，0 跳过。包含原包安装/激活、浏览器插件兼容与新增回归。
- `npm run build`：通过。
- `npm run build:web`：通过。
- `npm --prefix plugins/dsh-frontend-feedback run check`：构建、53 项测试和打包预检通过。
- PageCraft 集成测试原固定 3090 端口与开发服务冲突，已仅在测试临时 profile 中配置端口 0；复测通过，未关闭用户开发服务。
- 源码 diff 空白检查通过。生成的 `lib/client.js` 有打包器产生的空行尾空格告警，未手工改写生成文件。
- 独立代码复核见 `host-fixes-review.md` 和 `pagecraft-empty-review.md`，修订后通过。

## 浏览器证据与保留状态

在用户的右侧浏览器 `http://localhost:5173/` 操作。

1. session-003 中选择试验仓库；目录显示 backend/frontend 并保存成功。
2. 在 session-003 和 session-002 输入不同 QA 草稿；相互切换无串用，整页刷新后 A/B 均恢复。仅清理了本次新增的 QA 草稿，未发送这些草稿。
3. 新建 session-004，PageCraft 初始显示“尚未打开预览”。未授权操作目录时文件区提示无连接；通过网页选择 Conduit 后出现真实文件树，预览继续为空，未改动项目文件。
4. session-004 完成一次纯文字请求，显示“执行完成”；第二次纯文字请求中点击停止，留下生成到第 7 项的部分清单和“操作已取消”。整页刷新后两者仍在，原有已完成回复未丢失。
5. session-004 保留为验收证据，没有删除已有会话。

## 范围与限制

- 试验仓库未安装依赖、未修改源码或配置、未提交或推送。真实 verifyCode 已从 npm 启动失败推进至脚本执行：vitest/vite 缺失导致退出码 1，测试/构建仍不能报告通过。Windows 子脚本的本地编码错误文本存在乱码，但命令、退出码及依赖名称仍可识别；未将此扩展为本轮第七项修复。
- 契约检查是静态辅助分析，不是运行时接口测试。`getArticles.js:15` 的 `url[location]` 仍明确未解析；17/17 指可解析调用，不代表全部调用或所有端点已验证。后端静态入口和未被可解析调用引用的路由保留单独提示。
- 取消停止后续工作，但不回滚取消前已完成的外部副作用。重启恢复由临时目录测试覆盖，未为测试故意强杀用户开发服务。
- 保留本轮开始前的 PageCraft 未提交工作；本次未暂存、提交或推送任何变更。
