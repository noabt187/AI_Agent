# AI Agent Web Console 使用说明

## 项目简介

这个项目原本是一个命令行版 AI 编程 Agent 原型，现在已经扩展成带前后端的本地 Web 控制台。

它的目标是支持端到端前后端项目开发：用户可以在浏览器里输入需求，Agent 会读取代码、分析需求、设计方案、生成或修改文件，并通过流式输出把过程展示在前端。

目前保留了原来的命令行入口，同时新增了 Web 后端、React 前端、前端页面预览、元素评论和本地目录选择能力。

## 本次主要更改

### 1. 新增 Web 控制台

新增 `web/` 前端项目，使用：

- React
- Vite
- TypeScript
- lucide-react 图标

Web 控制台支持：

- 会话列表
- 新建会话
- 聊天输入
- Agent 流式输出
- 确认需求 / 确认方案
- 停止当前任务
- 设置操作目录
- 前端预览
- 对预览页面元素写评论
- 将元素评论批量发送给 Agent

### 2. 新增本地 Web 后端

新增 `src/server/`，提供本地 HTTP 服务：

- `GET /api/health`
- `GET /api/sessions`
- `POST /api/sessions`
- `GET /api/sessions/:sessionId`
- `POST /api/sessions/:sessionId/paths`
- `POST /api/sessions/:sessionId/abort`
- `POST /api/sessions/:sessionId/stream`
- `GET /api/preview?url=...`
- `GET /api/filesystem/directories?path=...`

后端会复用现有 `Orchestrator` 和 `Agent`，不是重新写一套 Agent。

### 3. Agent 支持事件式输出

修改了：

- `src/orchestrator/types.ts`
- `src/orchestrator/agent.ts`
- `src/orchestrator/orchestrator.ts`

现在 Agent 在保留命令行输出的同时，也能把以下事件推给 Web 前端：

- 普通输出
- 模型增量文本
- 工具调用
- 工具结果
- 最终结果
- 错误信息

### 4. 支持前端页面预览和元素评论

新增：

- `src/server/preview.ts`
- `web/src/annotationPrompt.ts`

使用方式：

1. 在左侧“前端预览”输入目标前端地址，例如 `http://localhost:3000`
2. 点击打开预览
3. 切换到“预览”
4. 点击右上角“评论”
5. 在预览页面里点击某个元素
6. 写下评论
7. 评论进入左侧评论列表
8. 点击“发送给 Agent”

发送给 Agent 的内容会包含：

- 页面 URL
- 标签名
- CSS selector
- DOM 路径
- 可见文本
- 元素位置和尺寸
- 用户评论

这样 Agent 能更精细地定位前端修改需求。

### 5. 支持本地文件夹选择操作目录

新增：

- `src/server/fileBrowser.ts`

左侧“操作目录”现在可以通过弹窗浏览本机文件夹。选择后会把绝对路径传给后端，并写入当前会话的 `allowedPaths`。

### 6. 新增测试

新增：

- `tests/preview-annotation.test.ts`

覆盖：

- 预览 HTML 是否正确注入标注脚本
- 元素评论是否能生成结构化 Agent prompt
- 目录浏览接口是否只返回子文件夹和绝对路径

## 项目结构

```text
AI_Agent/
  config/
    model.json                 # 本地模型配置，通常不提交到仓库

  scripts/
    orchestrator-chat.ts        # 原命令行入口

  src/
    QueryEngine.ts              # 对话状态和模型调用循环
    context/                    # 模型配置、上下文压缩
    llm/                        # OpenAI-compatible / Anthropic 客户端
    orchestrator/               # Agent 编排核心
    server/                     # 新增 Web 后端
    skills/                     # 需求分析、方案设计、代码生成提示词
    state/                      # 会话持久化
    tools/                      # 文件读取工具
    types/                      # 通用类型
    utils/                      # 工具函数

  tests/
    preview-annotation.test.ts  # 新增测试

  web/
    index.html
    package.json
    vite.config.ts
    src/
      App.tsx                   # Web 控制台主界面
      api.ts                    # 前端请求封装
      annotationPrompt.ts       # 元素评论转 Agent prompt
      main.tsx
      styles.css

  package.json
  tsconfig.json
  free_readme.md
```

## 环境要求

建议使用：

- Node.js 22 或更高版本
- npm

项目使用 npm workspaces，根目录管理后端和前端依赖。

## 拉取项目后的首次配置

### 1. 安装依赖

在项目根目录执行：

```bash
npm install
```

### 2. 配置模型

项目需要 `config/model.json`。这个文件包含 API key，默认应该被 `.gitignore` 忽略，不建议提交到仓库。

如果仓库里没有这个文件，需要手动创建：

```bash
mkdir -p config
```

然后创建 `config/model.json`：

```json
{
  "provider": "openai",
  "base_url": "https://your-openai-compatible-endpoint/v1",
  "api_key": "your-api-key",
  "model": "your-model-name",
  "anthropic_version": "2023-06-01",
  "context": {
    "projectRoot": ".",
    "projectIntroPath": "PROJECT_INTRO.md",
    "autoGenerateDirectoryTree": true,
    "maxContextChars": 12000,
    "retrieveTopK": 5
  }
}
```

如果使用 Anthropic 风格接口：

```json
{
  "provider": "anthropic",
  "base_url": "https://api.anthropic.com",
  "api_key": "your-api-key",
  "model": "claude-model-name",
  "anthropic_version": "2023-06-01"
}
```

注意：不要把真实 API key 提交到公开仓库。

## 启动方式

### Web 版，一键启动

在项目根目录执行：

```bash
npm run dev
```

它会同时启动：

- 后端服务：`http://localhost:3001`
- 前端页面：`http://localhost:5173`

浏览器打开：

```text
http://localhost:5173
```

### 分开启动

也可以分开启动：

```bash
npm run dev:server
```

```bash
npm run dev:web
```

### 命令行版

原命令行入口仍然保留：

```bash
npm run orchestrator
```

指定会话 ID：

```bash
npm run orchestrator -- session-001
```

## Web 控制台使用流程

### 1. 创建或选择会话

左侧会显示已有会话。点击 `+` 可以新建会话。

会话数据保存在：

```text
state/session-xxx/
```

包括：

- `messages.json`
- `orchestrator-state.json`
- `messages-full.json`
- `compression-failures.json`

### 2. 选择操作目录

在左侧“操作目录”区域点击“选择文件夹”。

选择后，系统会把该文件夹的绝对路径写入当前会话的 `allowedPaths`。Agent 之后只会在允许目录中读写文件。

### 3. 输入需求

在底部输入框输入需求，例如：

```text
帮我给这个前端项目的文章卡片增加阅读量展示
```

Agent 会按流程工作：

1. 读取相关代码
2. 输出需求分析
3. 等你确认
4. 输出实现方案
5. 等你确认
6. 修改代码
7. 输出完成总结

### 4. 前端预览和元素评论

如果你要精细修改某个前端元素：

1. 先启动目标前端项目，比如 `http://localhost:3000`
2. 在 Agent Console 左侧“前端预览”输入这个地址
3. 点击打开预览
4. 切换到“预览”
5. 点击右上角“评论”
6. 点击预览页面中的目标元素
7. 在底部输入评论
8. 点击“添加评论”
9. 多条评论都准备好后，点击“发送给 Agent”

Agent 会收到带定位信息的结构化需求。

### 5. 停止任务

如果 Agent 正在运行，可以点击右上角“停止”。

## 测试和构建

### 运行测试

```bash
npm test
```

### 构建后端 TypeScript

```bash
npm run build
```

### 构建前端

```bash
npm run build:web
```

建议在提交前至少运行：

```bash
npm test
npm run build
npm run build:web
```

## 当前能力边界

### 已支持

- 本地 Web 控制台
- 多会话
- 本地操作目录选择
- Agent 流式返回
- Agent 中断
- 前端页面预览
- 对预览页面元素做评论
- 将元素评论转成 Agent 需求
- 命令行入口保留

### 第一版限制

- 预览代理主要面向本地开发页面
- 第一版只对 HTML 页面注入标注脚本
- 第三方 iframe 不保证可标注
- 复杂登录态远程页面不保证稳定
- 还没有内置 diff 预览
- 还没有用户系统或权限系统

## 常见问题

### 1. `npm run dev` 后打不开页面

检查两个端口：

```text
http://localhost:3001
http://localhost:5173
```

如果端口被占用，可以先停止旧进程，再重新运行：

```bash
npm run dev
```

### 2. Agent 返回模型错误

检查：

- `config/model.json` 是否存在
- `api_key` 是否正确
- `base_url` 是否正确
- `model` 是否可用
- `api_key` 中是否有多余空格

### 3. Agent 无法修改目标项目

先确认左侧“操作目录”选择的是目标项目根目录，而不是当前 Agent Console 项目目录。

### 4. 前端预览打不开

确认目标前端项目已经启动，例如：

```text
http://localhost:3000
```

然后在“前端预览”里填入这个地址。

### 5. 元素评论没有选中目标

确认已经切到“预览”页，并开启右上角“评论”模式。

## 提交仓库前建议

不要提交以下内容：

- `node_modules/`
- `web/dist/`
- `dist/`
- `state/session-*`
- `config/model.json`
- `.env`

这些已经在 `.gitignore` 中有相应规则或应该被忽略。

如果需要给别人运行，请提交：

- 源码
- `package.json`
- `package-lock.json`
- `web/package.json`
- `free_readme.md`

别人拉取后执行：

```bash
npm install
```

再创建自己的 `config/model.json`，最后：

```bash
npm run dev
```
